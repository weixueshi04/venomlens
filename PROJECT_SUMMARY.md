# VenomLens 一拍知蛇 — 项目总结

> 影石 BoldMaker 2026 参赛原型 · 2026-09-23

## 项目定位

**一拍知蛇（VenomLens）** 是一个蛇伤辅助记录原型，从 Insta360 相机获取照片，辅助查看蛇种候选，并提供伤情记录与就医信息。

**重要声明**：当前是开发联调版本，不是可用于医疗判断的成品。模拟结果与照片无关；"未检测到蛇"不表示现场安全。候选与供应商原始分值不是物种确认或准确率，不能排除危险或替代医疗判断。

## 核心功能模块

### 1. 影像采集（M1）
- **Insta360 SDK 集成**：复用官方 Camera SDK 2.1.5 的连接、拍摄、下载能力
- **本地图片导入**：支持系统选图器，自动校正方向、JPEG 重编码、去除 EXIF/GPS 元数据
- **图片限制**：2 MB 上传限制，原图最多 32 MiB 本地存储
- **真机验证**：Xiaomi 13 Pro（Android 16）+ Insta360 X5（固件 v1.13.21），已实测 SDK 连接、普通后镜头拍照、下载、JPEG 导出

### 2. 识别服务（M2）
- **FastAPI 代理**：Python 后端，默认 mock 模式，真实调用预算为 0
- **HHodata 集成**：爬行类识别 API，最多返回 3 个本地已核验物种候选
- **安全边界**：
  - 上传需明确同意，仅接受回环地址（127.0.0.1）
  - 不自动轮询、重试或切换模型
  - pending 状态只允许人工 refresh
  - 供应商原始分值保留但不显示为准确率
- **离线能力**：48 项离线回归测试通过，不访问供应商

### 3. 交互与病例卡（M4）
- **Mock 识别页面**：红色 MOCK 标注，7 种固定响应场景（单候选、多候选、无法判断、未检测到蛇、处理中、上游超时、模型输出违规）
- **通用蛇伤病例模板**（2026-09-23 新增）：
  - 咬伤时间、部位、症状（最多 1000 字）
  - 年龄、手填位置（不获取定位）
  - **血型**（最多 10 字符）
  - **紧急联系人**：姓名（60 字）、电话（30 字）、关系（30 字）
  - 用户比对选择（默认"未比对"，不确诊）
  - 识别快照（pending、失败、未识别均可保存）
- **本地存储**：仅放在 `noBackupFilesDir`，不上传、不自动定位、不提供诊断
- **分支处理**：被咬/未咬均可进入记录，原图失败时可选择不附图继续

### 4. 医院查询（M5）
- **离线目录**：包含离线目录校验、复制和系统拨号界面
- **安全限制**：
  - 生产医院列表为空，等待有来源的核验资料
  - 不提供实时库存、距离或自动呼叫
  - 复制号码时标记为敏感信息（Android 13+）
- **当前状态**：框架已实现，数据待补充

### 5. 物种比对资料
- **离线可查**：中文观察要点、易混折叠、禁止行为、参考图署名
- **手动 HTTPS 链接**：不自动打开浏览器，需用户明确操作
- **数据来源**：`data/species.json` 带审核状态，不是医疗知识库
- **待复核**：颈棱蛇 2 张图的文件名与观察页记录冲突，暂缓打包

## 技术架构

### Android 端
- **语言**：Kotlin 2.3.20，JVM target 17
- **构建**：AGP 8.7.3，Gradle 8.12（wrapper 8.11.1 因超时未使用）
- **SDK**：compile/target 35，min 29（Android 10+）
- **架构**：MVVM，DataBinding，协程
- **关键模块**：
  - `care/`：病例记录（CaseRecord、CaseRecordStore、CaseRecordActivity）
  - `recognition/`：识别契约与 Adapter
  - `hospital/`：医院目录与拨号
  - `species/`：物种比对资料
  - `ui/`：连接、图库、设置等 ViewModel
  - `util/`：相机 WiFi 网络绑定、工具类

### Python 代理
- **框架**：FastAPI + Starlette 1.6.0
- **功能**：HHodata adapter、图片清理、物种映射、SQLite 预算账本、去重与缓存
- **测试**：50/50 离线回归通过（Conda Python 3.11）
- **部署**：仅监听 127.0.0.1:8765，不开放公网

### 数据与契约
- **识别契约**：`contracts/recognition-contract.md`，7 份模拟响应 fixtures
- **物种数据**：`data/species.json`，7 张引用图通过文件、署名和来源页核对
- **医院数据**：`data/hospitals.json`（待补充）

## 测试与验证

### 构建与单元测试
- **Android JVM**：55/55 通过
- **Python 离线**：50/50 通过
- **构建产物**：APK 约 278 MB（含 SDK 原生库、算法与示例资源）

### 模拟器仪器测试
- **Android 16 模拟器**：50 测试完成，49 通过，1 跳过（HTTP mock），1 失败（剪贴板测试，emulator 环境问题，非功能缺陷）
- **覆盖场景**：HTTP 7 场景、pending 手动查询、回环明文策略、病例部分提交恢复、医院安全拨号 Intent、物种比对资料、病例模板字段持久化

### 真机验证
- **已验证**：SDK 连接、普通后镜头拍照、下载、JPEG 导出、系统选图、USB reverse 的 HTTP mock
- **未验证**：公网代理、真实识别、全景取图、物理按键、长期稳定性
- **已知问题**：曾出现 `-2110103` 断开后重连成功；相机 Wi-Fi 与蜂窝网络同时开启时，下载误走蜂窝而超时的问题已修复

### 人工测试
- 操作系统选图器选择合成图片
- 保存并重开带原图的测试信息卡
- 查看未咬分支及医院空目录
- 复制号码（未实际拨打电话）
- **注意**：这些是模拟器与模拟数据验证，不是真机相机或真实识别验收

## 交付物

### 已发布
- **v0.1-dev Release**：APK 固定提交 `2e51bca7e2ec92b9838fd7d4f7efc73d9705742a`
- **SHA256 校验**：`SHA256SUMS.txt`
- **应用 ID**：`com.insta360.kmpsdk.demo`（可能与官方 Demo 冲突）
- **架构**：arm64-v8a，debug 签名

### 文档
- 中英双语 README
- 识别契约文档
- 开发规划（PLAN.md）
- 交接记录（docs/handoff/）

## 当前限制与未完成

### 功能限制
1. **真实识别未接入**：HTTP Adapter 尚未打通相机拍照后的识别流程
2. **医院数据为空**：生产医院列表等待有来源的核验资料
3. **物种比对待复核**：部分图片文件名与记录冲突
4. **包体未优化**：278 MB APK 包含完整 SDK，功能完成后再精简

### 技术债务
1. **远程交接路径**：7 份交接文件的非法 Git 路径尚待远程修复
2. **Git 历史合并**：队友 34 个文件已导入，但未合并 Git 分支历史
3. **真机相机链路**：完整端到端流程（相机→识别→病例卡）未真机验收
4. **物种资料审核**：名称核验不等于医疗/比对文案审核

### 安全与合规
1. **不提供诊断**：所有页面明确标注"不生成诊断""风险未知，须由医师判断"
2. **不上传原图**：原图仅存本机私有目录，卸载或清除应用数据会丢失
3. **不自动定位**：位置信息需用户手填
4. **不调用真实供应商**：默认 mock 模式，真实调用预算为 0

## 关键决策

### 技术选型
- **复用 SDK Demo**：不重写连接/拍摄/下载，节省时间
- **HHodata 代理**：不上 VLA、不自训练小模型、不配置自动模型兜底
- **离线优先**：所有关键功能可离线使用，网络仅用于可选的识别上传
- **本地存储**：病例卡仅存 `noBackupFilesDir`，不云同步

### 安全边界
- **明确同意**：上传前需用户明确操作，不自动上传
- **回环限制**：HTTP mock 代理只接受 127.0.0.1
- **预算控制**：真实调用预算为 0，SQLite 持久账本防绕过
- **敏感标记**：剪贴板内容标记为敏感（Android 13+）

### 演示策略
- **红色 MOCK 标注**：所有模拟页面明确标注非真实识别
- **不隐瞒限制**：README 和页面文案明确说明当前边界
- **分离验证**：构建/单元测试、模拟器运行、真机运行分别记录，不混为一谈

## 团队协作

### 分工
- **韦（本机）**：Android 关键路径（M1/M4/M5）、识别 Seam、集成测试
- **郭**：FastAPI 代理、物种数据、识别契约
- **舒**：医院数据搜集（待接入）

### 整合状态
- 队友 34 个正常路径代码/数据文件已按 Git blob SHA 核验并导入
- Python 离线回归 50/50 通过
- 内容整合完成，Git 分支历史未合并

## 后续建议

### 优先级 1（演示前）
1. 补充医院数据并校验
2. 完成物种比对资料复核
3. 真机端到端流程验收（相机→识别→病例卡）

### 优先级 2（演示后）
1. 精简 APK 包体（分析 SDK 占比，裁剪无用资源）
2. 合并 Git 分支历史
3. 修复远程交接路径

### 优先级 3（长期）
1. 接入真实识别供应商（需授权、预算、合规审查）
2. 医疗文案审核
3. 实景效果评估（188 张蛇图验证）

## 环境要求

### 构建环境
- **Android SDK**：platforms 33–36，build-tools 35.0.0/36.1.0
- **Android Studio**：JBR 21（编译目标 17）
- **Gradle**：8.12（wrapper 8.11.1 因超时未使用）
- **Python**：3.11（Conda 环境 insta360-reptile）
- **磁盘**：约 10 GB（SDK + 构建产物 + 依赖）

### 运行环境
- **Android**：10+（API 29+），arm64-v8a
- **相机**：Insta360 X5（固件 v1.13.21 已验证）
- **网络**：相机 Wi-Fi + 手机蜂窝，或 USB reverse 转发

## 总结

**一拍知蛇** 在 24 小时内完成了从 SDK Demo 到可运行原型的改造，实现了：

✅ 完整的影像采集链路（SDK 连接 + 本地导入）  
✅ 识别服务框架（mock + HTTP adapter，安全边界清晰）  
✅ 通用蛇伤病例模板（含用户信息收集，本地存储）  
✅ 医院查询入口（离线目录，安全拨号）  
✅ 物种比对资料（离线可查，手动链接）  
✅ 55 项 Android 单元测试 + 50 项 Python 测试 + 50 项仪器测试  

**核心原则**：
- 不提供诊断，不替代医疗判断
- 不上传原图，不自动定位
- 明确标注模拟模式，不隐瞒限制
- 分离验证层级，不混为一谈

**当前状态**：开发联调版本，可演示交互流程，不可用于真实蛇伤判断。真机相机端到端流程、真实识别、医院数据仍需补充。

---

*项目总结生成时间：2026-09-23*  
*基准提交：2e51bca7e2ec92b9838fd7d4f7efc73d9705742a*  
*测试设备：Xiaomi 13 Pro（Android 16）+ Insta360 X5*
