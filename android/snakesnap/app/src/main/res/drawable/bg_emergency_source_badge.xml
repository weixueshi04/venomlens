<?xml version="1.0" encoding="utf-8"?>
<!-- LIVE / CACHE 来源标注：晨雾浅底 + 低饱和描边，克制但仍常驻可见。
     MOCK 来源改用 bg_emergency_mock_badge（红底白字），见 EmergencyFlowFragment。 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/emergency_help_soft" />
    <stroke
        android:width="1dp"
        android:color="@color/emergency_mist" />
    <corners android:radius="6dp" />
</shape>
