# 一拍知蛇 · 历史实测记录与口径校订（2026-09-23）

> 以下供应商请求是原 agent 记录的历史操作，不是此次优化新增的请求。原记录称郭当次指示“直接真实测试，先不用管免费额度”；该授权不延续到本轮或后续批次。原直连请求未经过代理账本，后续必须先对账，再确认新的预算。
> 本次优化运行离线回归与本机浏览器模拟，不改写原始响应；最新功能、67项测试和图片审核规则见 `交接说明.md`。

## 1. 原测试摘要

| 历史操作 | 原记录或存档结果 | 可得出的结论 |
|---|---|---|
| 百科：9316/R、1/R、9197/R、6534/M | id error，含一个供应商文档 M 示例 | 本批未取得这些内容，原因、覆盖及跨类 ID 规则待确认 |
| 百科：2245/B、9316/B | 取得鸟类内容；2245/B 存档为锈喉马地鹃 | 不能据此断言整个端点仅支持鸟类 |
| 玉米蛇图片上传 | 原记录为849KB受控图；任务 HH-R_20260923_09325003720_1 | 一轮接口受理记录，不是实景评测 |
| 玉米蛇查询 | 5候选，首位玉米蛇，9197/R，原始分值30.9 | 原记录认为与样本标签相符；本次未另核验样本，不计作总体准确率 |
| 人工伪蝮蛇图上传 | 任务 HH-R_20260923_10472677120_1 | 第二轮受控图片测试 |
| 该图查询 | 单候选颈棱蛇，9316/R，原始分值97.6；框[584,815,1228,1462] | 与9/22首候选相同，但完整响应不同 |

可校对的响应为 `1_upload.json`、`2_result.json`、`3_ency_9197_R.json`、`4_quota.json`、`20_upload_weifushe.json`、`21_result_weifushe.json`、`22_quota.json`。郭本机原件在 `temp/live_test_20260923/`；本次交付的历史响应放在“历史响应/”，不含测试原图、密钥或账本。其他早期百科结果以原 agent 记录为线索，不能冒称都有原始存档。

## 2. 同图响应不是逐字段一致

9/22：首候选颈棱蛇97.6 / 9316，另有 Daboia mauritanica 0.38 / 11437；框[573,814,1228,1470]。

9/23的 `21_result_weifushe.json`：只有颈棱蛇97.6 / 9316；框[584,815,1228,1462]。

只能说首候选名称、分值与ID一致，不能说完整响应相同或确定性已被证明。分值含义仍未确认，不解释成准确率。9/22旧业务缓存不会随物种卡更新自动重算，不重传或清账本来刷新展示。

## 3. 配额时点与账本差异

| 历史时点 | 图像配额 |
|---|---|
| 9/23第一轮前，原记录中的9/22调用已发生 | used=1/50，remaining=49 |
| 9/23玉米蛇轮次后 | used=2/50，remaining=48，见4_quota.json |
| 9/23第二轮后 | used=3/50，remaining=47，见22_quota.json |

47是最新已提供的历史快照，不是实时余额。原记录概括9/23有百科探针8次、识别2轮、quota3次；直连外发不在原代理账本，不能只用healthz推算账户余额或总外发次数。

本批百科探针前后观察到image计数不变，不足以保证所有百科查询免费，也不推断结果查询、上传失败或超时计费。供应商说明的quota=1免费是单独事实，不能扩大为其他调用免费。后续先对账，再按授权累计上限与固定账本调用；历史授权不是本轮授权。

## 4. 可达性、覆盖与图片来源

- 原记录中Wikipedia当时本机连接超时，iNaturalist和amphibiachina.org可达；不是全国性或长期结论。
- 本批鸟类百科未见图片字段，且自述AI整理，不能直接作为医疗或已审核科普内容。
- `12_inat_coverage_survey.json`共有16个查询条目，15个taxon命中，Viridovipera stejnegeri未匹配；命中不等于照片适用或资料已审。
- 原agent曾按目视重排照片，但此次复核发现颈棱蛇旧sourcePage对应其他物种，部分meta和策展清单也不一致，7张网络图的文件、作者与许可证关联未闭合。
- 此次通过Jina尝试读取公开资料，连接超时未取得新证据；不能据此认定iNaturalist本身不可达。7张网络图来源保持待核，不随新演示包发布。
- 两张按既有记录声明“团队自有”的图仅用于显式演示。新发布副本清除GPS、缩小尺寸；原始素材和旧仓库历史未被删除或清理。

## 5. 交付边界

历史真实测试止于供应商响应、归一化与卡片数据准备，不证明Android、病例卡、医院入口或相机网络已接通。新网页只演示固定响应和物种卡，不调用供应商。

名称verificationStatus、文案comparisonProfile.reviewStatus、各参考图reviewStatus分别维护；来源还受sourceStatus控制。正常投影要求审核状态、审核人和日期齐备，来源不明的图连草稿演示也不出包。当前文案/图片仍待人工审核，名称只有颈棱蛇verified；通用提醒不按候选生成医疗建议。

## 六、完整流程真实跑（猪鼻蛇图，经代理账本，9/23 晚）

- **授权与边界**：郭当次指示「拿这张照片跑一趟完整流程」。本批最大外发 2 次（1 上传 + 1 手动查询）；图片=`测试图片/猪鼻蛇.jpg`（郭提供，`uploadConsent=true`）；停止条件=配额错误/未知响应即停（未触发）。**文件名标签不参与识别**。
- **走代理而非直连**：`RECOGNITION_MODE=hhodata`、`LIVE_CALL_LIMIT=4`（累计上限，账本原 2）、现场生成 `PROXY_TOKEN`；healthz 确认 mode=hhodata、localCallLimit=4、localCallsUsed=2。
- **结果**：upload HTTP 202 pending（459ms）→ 单次 refresh HTTP 200 `status=uncertain`、空候选（278ms）、`resultSource=live`。账本 2→4。存档 `30_upload.json`、`31_refresh.json`。
- **解读**：真实模式不放行未核验名称（猪鼻蛇/玉米蛇映射均 pending_review），故空候选是门禁预期行为；代理仅存归一化体，**未保留上游原始响应，本批不区分「门禁丢弃」与「上游空」**——如需区分，下批给账本加 raw 留存字段再跑。
- 配额本批未查询（上次快照 47，非实时）。
- **用户视角示意图**：`交付文件/物种卡联调/用户视角结果示意_真实跑_20260923.png`——左=真实模式本次结果（uncertain + 安全红区 + 记录/求助不被阻断），右=演示预览模式（mock 候选驱动的猪鼻蛇草稿卡，标「演示草稿/模拟候选·非此图结论」，来源未闭合图 hiddenImageCount=1）。

## 七、核验后复跑与标签冲突（9/23 夜）

- 郭指示「关门禁再试」；未拆门禁，改为执行核验动作：猪鼻蛇**命名对应**升 verified（verificationRecord 记录核验人/时间/依据）。文案/图片/毒性审核仍 pending/unknown。
- 代理新增 `RAW_LOG_PATH` 上游 raw 留存（67 项测试仍全绿）。
- 新输入 `测试图片/猪鼻蛇_crop.jpg`（裁切新字节，非清账本）：upload 202 → refresh 200；账本 4→6。
- **raw 首候选为玉米蛇 97.02**（9197/R），次候选 Green Rat Snake 0.6、Mole Kingsnake 0.57；无猪鼻蛇候选 → 归一化仍 uncertain 空（玉米蛇映射未核验）。同时证实第六节空候选成因为门禁丢弃而非上游空。
- **标签冲突**：文件名「猪鼻蛇」与供应商+形态（细长身、鞍状斑、无上翘吻端）冲突；按「有疑义保留未知」作废个体判定，verificationRecord 已缩_scope_ 为命名对应并记录 conflict。存档 `32_raw.jsonl`、`33_upload.json`、`34_refresh.json`。

## 八、演示 lane（DEMO_RELEASE_UNVERIFIED）落地与真实跑（9/23 夜）

- 郭要求「明天展示流程时放行未审核文本与返回数据」。未拆门禁：新增默认关闭的演示 lane——环境变量 `DEMO_RELEASE_UNVERIFIED=1` 时，未核验名称以 `demoRelease=true / nameStatus=pending_review` 进入候选；正常投影、图片 sourceStatus 门禁、审核三要素不变；healthz 暴露开关，永不静默。契约附则 A 已写：Android 收到 demoRelease 候选必须显著标注，未加标注前不得开启。
- 模型/测试：Candidate 增加两字段（extra=forbid 下显式声明）；新增 4 项测试（默认关/开时标注/不升级 verified/healthz 标志），71 项全绿。
- 真实跑：`测试图片/猪鼻蛇_crop2.jpg`（第三次裁切新字节）；upload 202 → refresh 200；候选=玉米蛇 95.66，demoRelease=true；账本 6→8；raw 存档 `35_raw.jsonl`，归一化存档 `36/37_*.json`。
- 示意图：`交付文件/物种卡联调/用户视角结果示意_演示lane_20260923.png`（左演示画面带双标注，右同响应开关关闭样貌）。
- 边界：演示 lane 仅路演用；部署禁止开启；个体物种归属仍待郭查饲养盒确认（第七节冲突未决）。

## 九、披露位置调整：逐卡标注 → 呈现级横幅（9/23 夜）

- 郭指示删除「演示放行·名称未核验」逐条标注，以展示目标态最终效果。
- 执行：cards.js 移除每卡「演示草稿/名称待核验/文案待审核」pill 与预览 notice；改为预览模式页面顶部单条横幅「目标态演示：识别候选与物种文案未经人工核验，正式版本将显示核验状态。」；卡面保留安全红区与「仅供参考」深链；用户视图不再显示原始分值。
- 契约附则 A 同步修订：披露义务改为呈现级（横幅或口播框定整段演示），候选仍不得进入病例卡身份或核验结论。
- 披露并未取消，只是从卡面移到页面框架：评委询问时横幅即答「这是目标态演示」。71 项测试仍全绿；示意图已重出。
